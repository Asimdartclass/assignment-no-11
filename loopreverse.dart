void main() {
  // Start with the largest number in the range
  int number = 10;

  // Print numbers in reverse order using a while loop
  while (number > 0) {
    print(number);
    number--; // Decrement the number
  }
}
